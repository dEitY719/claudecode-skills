# statusline-setup — help

Paste the locator block from `references/run-script.md`, then run

```
sh "$_SL" --help
```

and print its output verbatim, then stop.

The script's `usage()` is the single source of truth for the accepted flags.
Do not copy that text into this file — a second copy drifts.
