# cache-ttl — help

Run

```
sh "${CLAUDE_PLUGIN_ROOT:-.}/skills/cache-ttl/scripts/set-cache-ttl.sh" --help
```

and print its output verbatim, then stop.

The script's `usage()` is the single source of truth for the accepted
arguments. Do not copy that text into this file — a second copy drifts.
